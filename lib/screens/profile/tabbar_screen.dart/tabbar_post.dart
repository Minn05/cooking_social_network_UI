import 'package:flutter/material.dart';

class TabBar_Post extends StatelessWidget {
  const TabBar_Post({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('TabBar_Post'),
    );
  }
}